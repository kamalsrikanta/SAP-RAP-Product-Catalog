projection;
strict ( 2 );
use draft;

define behavior for ZCJ_C_PRODUCT alias Product
{
  use create;
  use update;
  use delete;

  use action submitForApproval;
  use action approve;
  use action reject;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}
